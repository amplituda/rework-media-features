module.exports = function (options, css) {

    var hoverSelectorPrefix = options.hoverSelectorPrefix;
    if ((typeof hoverSelectorPrefix) !== 'string') {
      throw new Error('hoverSelectorPrefix option must be a string');

    return;
  }

}
